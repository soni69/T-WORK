"""
Конфигурация бота для отчётности по продажам.
"""
import os
from datetime import datetime
from zoneinfo import ZoneInfo
from dotenv import load_dotenv

load_dotenv()

# Текущая дата по Москве (для планов и сводки — один и тот же «сегодня»)
def today_moscow():
    return datetime.now(ZoneInfo("Europe/Moscow")).date()

# Токен бота от @BotFather
BOT_TOKEN = os.getenv("BOT_TOKEN", "")

# ID чата/группы для отправки отчётов (можно несколько через запятую)
REPORT_CHAT_IDS = [
    int(x.strip()) for x in os.getenv("REPORT_CHAT_IDS", "").split(",") if x.strip()
]

# Роли: РТТ (руководитель торговой точки) — может корректировать планы и сводку; ПК (продавец-консультант) — только заполнять отчёты и смотреть.
# ID пользователей с ролью РТТ (через запятую в .env). Остальные допущенные пользователи — ПК.
RTT_USER_IDS = set(
    int(x.strip()) for x in os.getenv("RTT_USER_IDS", "").split(",") if x.strip()
)

# Направления продаж (можно расширять)
# ВНИМАНИЕ: названия должны в точности совпадать с теми, что используются в отчётах и планах.
SALES_DIRECTIONS = [
    "Sim",                    # в шт
    "Золото/абонемент",       # в шт
    "Мнп",                    # в шт
    "Combo",                  # в шт
    "Страховки",              # в сумме
    "Аксессуары",             # в сумме
    "Смартфоны",              # в сумме
    "Услуги",                 # в сумме
    "Кредиты",                # в сумме
    "Заявка на кредит",       # в шт
    "Wink подписка",          # в сумме
    "Фокусное оборудование",  # в сумме
    "Домашний интернет",      # в шт
]

# Единицы измерения по направлениям: "шт" или "₽"
DIRECTION_UNITS = {
    "Sim": "шт",
    "Золото/абонемент": "шт",
    "Мнп": "шт",
    "Combo": "шт",
    "Страховки": "₽",
    "Аксессуары": "₽",
    "Смартфоны": "₽",
    "Услуги": "₽",
    "Кредиты": "₽",
    "Заявка на кредит": "шт",
    "Wink подписка": "₽",
    "Фокусное оборудование": "₽",
    "Домашний интернет": "шт",
}

# Сокращения направлений для краткого дневного отчёта
DIRECTION_SHORT = {
    "Sim": "Sim",
    "Золото/абонемент": "Золото",
    "Мнп": "Мнп",
    "Combo": "Combo",
    "Страховки": "Стрх",
    "Аксессуары": "Аксес",
    "Смартфоны": "Смарт",
    "Услуги": "Услуг",
    "Кредиты": "Кред",
    "Заявка на кредит": "Закр",
    "Wink подписка": "Wink",
    "Фокусное оборудование": "Фокус",
    "Домашний интернет": "ДомИнт",
}

# Логические блоки направлений для отчётов (в нужном порядке)
DIRECTION_BLOCKS = [
    [
        "Sim",
        "Золото/абонемент",
        "Мнп",
        "Combo",
    ],
    [
        "Страховки",
        "Аксессуары",
        "Смартфоны",
        "Услуги",
        "Кредиты",
        "Заявка на кредит",
    ],
    [
        "Wink подписка",
        "Фокусное оборудование",
        "Домашний интернет",
    ],
]

# Расписание дневных фокусных отчётов (часы в формате 24ч)
REPORT_SCHEDULE = [12, 16, 19]  # 12:00, 16:00, 19:00

# Путь к БД (по умолчанию — рядом со скриптом, чтобы не терять данные при разном текущем каталоге)
_DEFAULT_DB = os.path.join(os.path.dirname(os.path.abspath(__file__)), "sales_reports.db")
DATABASE_PATH = os.getenv("DATABASE_PATH", _DEFAULT_DB)
